<%
  active = 'view'
  title = 'Browse'
%>
<div class="container">
	<div class="row">
		<div class="col-md-12">

			<table class="table">
				<thead>
					<tr>
						<th>Date</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
				<% for(var k in logs) { %>
					<tr>
						<td><%= logs[k].createdAt %></td>
						<td>
							<a href="/view/<%= logs[k].id %>" class="btn btn-default">Link</a>
						</td>
					</tr>
				<% } %>
				</tbody>
			</table>

		</div>
	</div>
</div>